<script>
  import { page } from '$app/state'
</script>

<p>You're on <code class="text-sm">{page.url.pathname}</code>, this page has <code class="text-sm">trailingSlash</code> disabled.</p>
<p>Note that the <code>← back</code> button at the bottom of the page (which is the same on all pages) is broken.</p>
